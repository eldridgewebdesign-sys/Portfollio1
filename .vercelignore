# Keep internal / sensitive files out of the public Vercel deploy.
# (Vercel still installs /api deps from package.json — do NOT ignore that.)

# Internal role-system docs incl. the SECURITY LOG (audit F4) — must not be public.
docs

# Supabase schema + admin-setup SQL — leaks table/column/RLS structure if served.
db

# Local AI-assistant config (also gitignored) — belt-and-suspenders for CLI deploys.
CLAUDE.md
